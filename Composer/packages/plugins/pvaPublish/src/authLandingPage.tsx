import * as React from 'react';
import { RouteComponentProps } from '@reach/router';

export const AuthLandingPage: React.FC<RouteComponentProps> = () => {
  return (
    <div>
      <h1>Auth landing page</h1>
    </div>
  );
};
